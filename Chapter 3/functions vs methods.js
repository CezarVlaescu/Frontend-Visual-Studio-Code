const name = 'cezar';

// functions -> the defined on his own, not on an object or data time

const greet = () => 'hello'

let resultOne = greet();
console.log(resultOne);

// methods -> just functions that do something for us

let resultTwo = name.toUpperCase();
console.log(resultTwo);

// diff : how we call them and where are they defined